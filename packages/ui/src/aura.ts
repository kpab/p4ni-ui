// Separate entry point: requires the optional peers `three` and
// `@react-three/fiber`. Kept out of the main entry so users without them
// can keep importing from "@p4ni/ui".
export { AuraInput } from "./AuraInput/AuraInput";
export type { AuraInputProps } from "./AuraInput/AuraInput";
